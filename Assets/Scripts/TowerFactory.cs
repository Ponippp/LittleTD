//TODO:
/*
Use factory pattern to create different types of towers.

Also uses constructor DI because it creates towers using params instead of having each tower subclass
decide them internally (ie we set dmg here instead of in each tower subclass)
*/